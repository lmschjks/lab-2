<!DOCTYPE html>
<html>
<head>
    <title>#{title}</title>
    <meta charset="utf-8" />
</head>
<body>
    
    <h1>#{title} в Pug</h1>
    
    <p>Имя: #{name}</p>
    <p>Возраст: #{age}</p>
    <p>Род занятий: #{occupation}</p>
    <p>Хобби: #{hobby}</p>

    <h2>Список интересов</h2>

    if kostyl

        <ul>
            each intrest in intrests
                <li>#{intrest}</li>
        </ul>
    else
        <h3>Нет</h3>
    <a href="imaginary_portfolio.com">Портфолио</a>
</body>
</html>